package com.bimoraai.brahm.core.theme

import androidx.compose.ui.graphics.Color

// ─── Exact mirror of tailwind.config.ts website colors ───────────────────────

// Backgrounds
val BrahmBackground   = Color(0xFFF7F7F8)  // bg-background (warm off-white)
val BrahmCard         = Color(0xFFFFFFFF)  // bg-card (pure white)
val BrahmSidebar      = Color(0xFFF0F0F2)  // bg-sidebar / bg-muted
val BrahmMuted        = Color(0xFFF0F0F2)  // bg-muted

// Text
val BrahmForeground       = Color(0xFF111827)  // text-foreground (near-black)
val BrahmMutedForeground  = Color(0xFF6B7280)  // text-muted-foreground (medium gray)

// Brand colors
val BrahmGold    = Color(0xFFB07A00)  // amber-700 — primary gold
val BrahmSaffron = Color(0xFFD4540A)  // orange-600 — secondary saffron/CTA
val BrahmPurple  = Color(0xFF7C3AED)  // violet-700 — mystic accent

// Borders
val BrahmBorder  = Color(0xFFE5E7EB)  // border-border (light gray)

// Status
val BrahmSuccess = Color(0xFF16A34A)  // green-600
val BrahmError   = Color(0xFFDC2626)  // red-600
val BrahmWarning = Color(0xFFD97706)  // amber-600
